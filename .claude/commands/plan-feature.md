---
description: Plan a feature or change for axis-website without editing code — scope, files, the SPA generated/hand-owned split, the validation gate, deploy/drift risk, and the mandatory Plane task-tracking step. Presents a plan and waits for approval.
---

Plan the following feature or change WITHOUT editing any code or files. Produce a
written plan only; make no edits until it is explicitly approved.

Feature request: $ARGUMENTS

Work through these steps and present the result:

1. **Goal & acceptance criteria.** Restate the goal in one or two sentences and
   list concrete, testable acceptance criteria.

2. **Placement — hand-owned vs. generated.** Identify which files the change
   touches and classify each:
   - **Hand-owned** (editable here): `worker.js`, `wrangler.jsonc`,
     `public/welcome.html`, `public/imprint.html`, `public/privacy.html`,
     `public/terms.html`, `public/style.css`, `public/_headers`.
   - **Generated by Axis's deploy-remote Action** (NEVER hand-edit here):
     `public/index.html`, `public/_app/**`, `public/service-worker.js`,
     `public/manifest.webmanifest`, the icons/favicon, `public/version.json`.
   If the change is really about the SPA content, it belongs in the **Axis** repo
   (rebuilt and republished here by deploy-remote) — say so and stop.

3. **Files to touch.** List the exact hand-owned files and what changes in each.

4. **Validation & deploy risk.** State how you will verify: (a) SPA presence
   (`public/index.html`, `public/welcome.html`, non-empty `public/_app/`) and
   (b) `WRANGLER_SEND_METRICS=false npx --yes wrangler@4 deploy --dry-run`. Note
   that `git push` here IS a production deploy via Cloudflare's git integration,
   and that hand-edit branches must be rebased onto `origin/master` before pushing.

5. **Task tracking (MANDATORY — see CLAUDE.md, "Task tracking" section).**
   Search this repo's Plane project (its identifier is in `CLAUDE.local.md`) for an
   existing work item covering this change (`search_work_items` / `list_work_items`).
   If none exists, create one with an imperative title and a description of goal +
   why + acceptance criteria. Move it to **In Progress** when implementation
   begins. State which item this maps to (existing id or "to be created").

Finally: present the full plan, then WAIT for approval. Do not make any edits.
